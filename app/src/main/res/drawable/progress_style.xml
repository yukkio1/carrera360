<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>
            <solid android:color="@color/colorProgressBackground"/> <!-- Fondo de la barra -->
        </shape>
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <solid android:color="@color/colorProgressFill"/> <!-- Color del avance -->
            </shape>
        </clip>
    </item>
</layer-list>
